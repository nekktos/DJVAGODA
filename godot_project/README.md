# ДжваГода — Godot project

Открывать эту папку (`godot_project/`) в Godot 4.x как проект.
Полный дизайн-документ и план этапов — на уровень выше: `../GDD.md`, `../HANDOFF.md`.

**Текущее состояние: Этап 0 — сетевой фундамент.** Контента нет и не должно быть,
пока сеть не подтверждена тестом вдвоём.

## Что уже есть

| Файл | Роль |
|---|---|
| `scenes/Main.tscn` | точка входа: grey-box мир + меню подключения поверх него |
| `scenes/World.tscn` | grey-box: плоскость 60×60, 4 цветных столба-ориентира, три ступени для проверки прыжка, `MultiplayerSpawner` |
| `scenes/Player.tscn` | `CharacterBody3D` + камера от третьего лица + `MultiplayerSynchronizer` |
| `scripts/network_manager.gd` | автолоад `Net`: ENet-хост/клиент, сигналы сессии |
| `scripts/world.gd` | спавн и деспавн персонажей |
| `scripts/player.gd` | движение и репликация трансформа |
| `scripts/main.gd` | меню, HUD, разбор аргументов командной строки |

## Управление

WASD — движение, Space — прыжок, мышь — обзор, Esc — освободить/вернуть курсор,
F10 — выйти в меню.

## Как запустить тест на двух окнах

Из корня репозитория:

```
powershell -ExecutionPolicy Bypass -File .\run_two_clients.ps1
```

Или вручную: запустить проект дважды, в первом окне нажать «Создать игру (хост)»,
во втором — «Подключиться» к `127.0.0.1`. Порт — 24545.

Аргументы командной строки (передавать после `--`):

- `--host` — сразу поднять хост
- `--join=АДРЕС` — сразу подключиться
- `--bot` — персонаж ходит по кругу сам (тест сети без второго живого игрока)
- `--netlog` — раз в секунду печатать позиции всех персонажей

## Архитектурные решения Этапа 0

- **Транспорт:** ENet (`ENetMultiplayerPeer`), P2P, хост = peer id 1. Ровно 2 игрока.
  Выделенных серверов и матчмейкинга нет — отдельная задача на будущее (GDD §9).
- **Авторитет над движением — клиентский.** Каждый пир симулирует только своего
  персонажа и реплицирует трансформ; остальные интерполируют. Всё остальное
  (урон, ресурсы, стройка, караван) на следующих этапах будет считать хост.
  Движение написано через `apply_input(снимок_ввода, delta)` — при переезде на
  host-авторитет меняется только источник снимка, а не сама симуляция.
- **Мир не подгружается после коннекта.** `World` лежит в главной сцене с самого
  старта на обоих пирах, поэтому `MultiplayerSpawner` уже существует к моменту
  соединения. Иначе клиент не успевает принять команду спавна.
- **Имя ноды персонажа == peer id владельца.** По нему `_enter_tree()`
  рекурсивно выставляет `set_multiplayer_authority()` на всех пирах.
- **Точка спавна — детерминированная функция peer id**, считается одинаково на
  всех пирах, поэтому стартовую позицию не нужно реплицировать отдельно.
